<Window x:Class="WpfAppScraper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:oxy="http://oxyplot.org/wpf"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppScraper" xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        mc:Ignorable="d"
        Title="Visualizer"
         WindowState="Maximized"
    WindowStartupLocation="CenterScreen">
    <TabControl>
        <TabItem Header="Patient Analysis">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Cancer Type Dropdown for Patient Bar Chart -->
                <ComboBox x:Name="CancerTypeDropdown" 
                          Grid.Row="0"
                          Margin="10"
                          SelectionChanged="CancerTypeDropdown_SelectionChanged"/>

                <!-- Patient Dropdown -->
                <ComboBox x:Name="PatientDropdown" 
                          Grid.Row="1" 
                          Margin="10" 
                          SelectionChanged="PatientDropdown_SelectionChanged"/>

                <!-- LiveCharts Chart -->
                <lvc:CartesianChart x:Name="chart" 
                                    Grid.Row="2" 
                                    Series="{Binding SeriesCollection}">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Labels="{Binding Labels}"/>
                    </lvc:CartesianChart.AxisX>
                </lvc:CartesianChart>
            </Grid>
        </TabItem>

        <TabItem Header="Heatmap Analysis">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Cancer Type Dropdown for Heatmap -->
                <ComboBox x:Name="HeatmapCancerTypeDropdown" 
                          Grid.Row="0"
                          Margin="10"
                          SelectionChanged="ShowHeatmap_Click"/>

                <!-- OxyPlot Heatmap -->
                <oxy:PlotView x:Name="HeatmapPlot"
                              Grid.Row="1"
                              Model="{Binding HeatmapModel}"
                              Margin="10"/>
            </Grid>
        </TabItem>
    </TabControl>

</Window>
